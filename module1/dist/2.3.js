"use strict";
{
    // const rollNumbers : number[] =[1,4,23];
    const rollNumbers = [1, 4, 23];
    // const mentors: string[] = ['Mr X', 'Mr Y', 'Mr Z'];
    const mentors = ['Mr X', 'Mr Y', 'Mr Z'];
    // const boolArray: boolean[] = [true, false, true];
    const boolArray = [true, false, true];
    const user = [
        {
            name: 'munir',
            age: 100
        },
        {
            name: 'Sirajul',
            age: 230
        }
    ];
    const add = (x, y) => x + y;
    add(30, 20);
    // 
}
